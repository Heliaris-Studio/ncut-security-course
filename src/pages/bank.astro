---
import Layout from '../layouts/Layout.astro';
import { parseQuizData } from '../lib/parse-quiz';

const quizData = parseQuizData();
---

<Layout title="資訊安全導論 — 題庫總覽">
  <script type="application/json" id="bank-data" set:html={JSON.stringify(quizData)} />

  <div class="container">
    <header class="app-header">
      <h1>題庫總覽</h1>
      <p>共 {quizData.questions.length} 題，{quizData.chapters.length} 個章節</p>
      <a href={import.meta.env.BASE_URL} class="btn btn-secondary back-link">
        <svg class="icon" viewBox="0 0 20 20" fill="none"><path d="M12 4L6 10L12 16" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></svg>
        回到測驗
      </a>
    </header>

    <div class="chapter-nav" id="chapter-nav">
      <button class="chapter-nav-link active" data-ch="all">全部</button>
      {quizData.chapters.map(ch => (
        <button class="chapter-nav-link" data-ch={ch.id}>
          CH {String(ch.id).padStart(2, '0')}
          <span class="nav-count">{ch.questionCount}</span>
        </button>
      ))}
    </div>

    <div class="search-bar">
      <svg class="search-icon" viewBox="0 0 20 20" fill="none" aria-hidden="true"><circle cx="9" cy="9" r="6" stroke="currentColor" stroke-width="1.8"/><path d="M13.5 13.5L17 17" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>
      <input
        type="search"
        id="search-input"
        class="search-input"
        placeholder="搜尋題目或選項關鍵字（按 Esc 清除）"
        autocomplete="off"
        spellcheck="false"
      />
      <button type="button" id="search-clear" class="search-clear" aria-label="清除搜尋" hidden>
        <svg viewBox="0 0 20 20" fill="none" aria-hidden="true"><path d="M6 6L14 14M14 6L6 14" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>
      </button>
      <span id="search-stats" class="search-stats" hidden></span>
    </div>

    <div id="question-list" class="question-list"></div>

    <div class="pagination" id="pagination"></div>
  </div>
</Layout>

<style is:global>
  .back-link {
    margin-top: var(--space-4);
    text-decoration: none;
    font-size: 0.8125rem;
  }

  .chapter-nav {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    margin-bottom: var(--space-6);
    position: sticky;
    top: 0;
    background: var(--color-bg);
    padding: var(--space-3) 0;
    z-index: 10;
    border-bottom: 1px solid var(--color-border);
  }
  .chapter-nav-link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-1);
    padding: var(--space-1) var(--space-3);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    font-size: 0.8125rem;
    font-weight: 600;
    color: var(--color-neutral-600);
    background: var(--color-surface);
    cursor: pointer;
    font-family: var(--font-sans);
    transition: border-color var(--transition-fast), color var(--transition-fast), background var(--transition-fast);
  }
  .chapter-nav-link:hover {
    border-color: var(--color-primary-500);
    color: var(--color-primary-600);
  }
  .chapter-nav-link.active {
    background: var(--color-primary-600);
    border-color: var(--color-primary-600);
    color: #fff;
  }
  .chapter-nav-link.active .nav-count {
    background: rgba(255,255,255,0.2);
    color: #fff;
  }
  .nav-count {
    font-size: 0.6875rem;
    background: var(--color-neutral-100);
    color: var(--color-neutral-500);
    padding: 0 var(--space-2);
    border-radius: 9999px;
  }

  .search-bar {
    position: relative;
    display: flex;
    align-items: center;
    gap: var(--space-2);
    margin-bottom: var(--space-4);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: 0 var(--space-3);
    transition: border-color var(--transition-fast), box-shadow var(--transition-fast);
  }
  .search-bar:focus-within {
    border-color: var(--color-primary-500);
    box-shadow: 0 0 0 3px var(--color-primary-50);
  }
  .search-icon {
    width: 18px;
    height: 18px;
    color: var(--color-neutral-400);
    flex-shrink: 0;
  }
  .search-input {
    flex: 1;
    border: none;
    outline: none;
    background: transparent;
    font-family: var(--font-sans);
    font-size: 0.9375rem;
    color: var(--color-neutral-800);
    padding: var(--space-3) 0;
    min-width: 0;
  }
  .search-input::placeholder {
    color: var(--color-neutral-400);
  }
  .search-input::-webkit-search-cancel-button {
    -webkit-appearance: none;
    appearance: none;
  }
  .search-clear {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 24px;
    height: 24px;
    padding: 0;
    border: none;
    border-radius: 9999px;
    background: var(--color-neutral-100);
    color: var(--color-neutral-500);
    cursor: pointer;
    flex-shrink: 0;
    transition: background var(--transition-fast), color var(--transition-fast);
  }
  .search-clear svg {
    width: 14px;
    height: 14px;
  }
  .search-clear:hover {
    background: var(--color-neutral-200);
    color: var(--color-neutral-700);
  }
  .search-stats {
    font-size: 0.75rem;
    color: var(--color-neutral-500);
    white-space: nowrap;
    flex-shrink: 0;
  }

  .bank-q-text mark,
  .bank-option mark {
    background: #fef08a;
    color: inherit;
    padding: 0 2px;
    border-radius: 2px;
  }

  .empty-state {
    text-align: center;
    padding: var(--space-8) var(--space-4);
    color: var(--color-neutral-500);
    border: 1px dashed var(--color-border);
    border-radius: var(--radius-lg);
    background: var(--color-surface);
  }
  .empty-state-title {
    font-size: 0.9375rem;
    font-weight: 600;
    color: var(--color-neutral-700);
    margin-bottom: var(--space-1);
  }
  .empty-state-hint {
    font-size: 0.8125rem;
  }

  .question-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    min-height: 200px;
  }

  .bank-question {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-4) var(--space-5);
  }
  .bank-q-header {
    display: flex;
    gap: var(--space-3);
    margin-bottom: var(--space-3);
  }
  .bank-q-num {
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 28px;
    height: 28px;
    border-radius: var(--radius-sm);
    background: var(--color-neutral-100);
    font-size: 0.8125rem;
    font-weight: 600;
    color: var(--color-neutral-600);
    flex-shrink: 0;
  }
  .bank-q-chapter {
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--color-primary-600);
    background: var(--color-primary-50);
    padding: var(--space-1) var(--space-2);
    border-radius: var(--radius-sm);
    white-space: nowrap;
  }
  .bank-q-text {
    font-size: 0.9375rem;
    font-weight: 600;
    color: var(--color-neutral-800);
    line-height: 1.6;
    padding-top: 2px;
  }

  .bank-options {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-2);
    margin-left: 40px;
  }
  .bank-option {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-size: 0.8125rem;
    color: var(--color-neutral-500);
    padding: var(--space-2) var(--space-3);
    border-radius: var(--radius-md);
  }
  .bank-option.is-answer {
    background: var(--color-success-50);
    color: var(--color-success-600);
    font-weight: 600;
  }
  .bank-opt-label {
    font-weight: 600;
    flex-shrink: 0;
  }

  .pagination {
    margin-top: var(--space-8);
    padding-bottom: var(--space-8);
  }
  .page-bar {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-4) var(--space-6);
  }
  .page-summary {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--space-3);
    padding-bottom: var(--space-3);
    border-bottom: 1px solid var(--color-border);
  }
  .page-summary-text {
    font-size: 0.8125rem;
    color: var(--color-neutral-500);
  }
  .page-summary-text strong {
    color: var(--color-neutral-800);
    font-weight: 600;
  }
  .page-summary-total {
    font-size: 0.75rem;
    color: var(--color-neutral-400);
  }
  .page-buttons {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-1);
  }
  .page-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 36px;
    height: 36px;
    padding: 0 var(--space-2);
    border: 1px solid transparent;
    border-radius: var(--radius-md);
    background: transparent;
    font-size: 0.8125rem;
    font-weight: 600;
    color: var(--color-neutral-600);
    cursor: pointer;
    font-family: var(--font-sans);
    transition: background var(--transition-fast), color var(--transition-fast);
  }
  .page-btn:hover:not(:disabled):not(.active) {
    background: var(--color-neutral-100);
  }
  .page-btn.active {
    background: var(--color-primary-600);
    color: #fff;
    border-color: var(--color-primary-600);
  }
  .page-btn:disabled {
    opacity: 0.3;
    cursor: not-allowed;
  }
  .page-ellipsis {
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 36px;
    height: 36px;
    font-size: 0.8125rem;
    color: var(--color-neutral-400);
    letter-spacing: 0.1em;
  }

  @media (max-width: 640px) {
    .bank-question {
      padding: var(--space-3) var(--space-4);
    }
    .bank-q-header {
      flex-wrap: wrap;
      gap: var(--space-1) var(--space-2);
      align-items: center;
    }
    .bank-q-num {
      min-width: 24px;
      height: 24px;
      font-size: 0.75rem;
    }
    .bank-q-chapter {
      font-size: 0.6875rem;
      padding: 2px var(--space-2);
    }
    .bank-q-text {
      font-size: 0.875rem;
      width: 100%;
      padding-top: var(--space-1);
    }
    .bank-options {
      grid-template-columns: 1fr;
      margin-left: 0;
      gap: 0;
    }
    .bank-option {
      font-size: 0.8125rem;
      padding: var(--space-1) var(--space-2);
    }
    .chapter-nav {
      gap: var(--space-1);
    }
    .chapter-nav-link {
      font-size: 0.75rem;
      padding: var(--space-1) var(--space-2);
    }
    .page-summary {
      flex-direction: column;
      gap: var(--space-1);
      text-align: center;
    }
    .page-buttons {
      flex-wrap: wrap;
      gap: var(--space-1);
    }
    .page-btn {
      min-width: 32px;
      height: 32px;
      font-size: 0.75rem;
    }
    .search-input {
      font-size: 0.875rem;
      padding: var(--space-2) 0;
    }
    .search-stats {
      display: none;
    }
  }
</style>

<script>
  const dataEl = document.getElementById('bank-data')!;
  const data = JSON.parse(dataEl.textContent!);
  const labels = ['A', 'B', 'C', 'D'];
  const PAGE_SIZE = 25;

  const listEl = document.getElementById('question-list')!;
  const paginationEl = document.getElementById('pagination')!;
  const navBtns = document.querySelectorAll<HTMLButtonElement>('.chapter-nav-link');
  const searchInput = document.getElementById('search-input') as HTMLInputElement;
  const searchClear = document.getElementById('search-clear') as HTMLButtonElement;
  const searchStats = document.getElementById('search-stats') as HTMLSpanElement;

  let currentChapter: string | number = 'all';
  let currentPage = 1;
  let searchQuery = '';

  function getFiltered() {
    let list = currentChapter === 'all'
      ? data.questions
      : data.questions.filter((q: any) => q.chapterId === Number(currentChapter));

    if (searchQuery) {
      const q = searchQuery.toLowerCase();
      list = list.filter((item: any) => {
        if (item.question.toLowerCase().includes(q)) return true;
        return item.options.some((o: string) => o.toLowerCase().includes(q));
      });
    }
    return list;
  }

  function escapeHtml(text: string): string {
    const d = document.createElement('div');
    d.textContent = text;
    return d.innerHTML;
  }

  function highlight(text: string): string {
    const safe = escapeHtml(text);
    if (!searchQuery) return safe;
    const esc = searchQuery.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
    return safe.replace(new RegExp(esc, 'gi'), m => `<mark>${m}</mark>`);
  }

  function render() {
    const filtered = getFiltered();
    const totalPages = Math.max(1, Math.ceil(filtered.length / PAGE_SIZE));
    if (currentPage > totalPages) currentPage = totalPages;

    updateSearchStats(filtered.length);

    if (filtered.length === 0) {
      listEl.innerHTML = `
        <div class="empty-state">
          <div class="empty-state-title">沒有符合的題目</div>
          <div class="empty-state-hint">${searchQuery ? '試試其他關鍵字或切換章節' : '此章節目前沒有題目'}</div>
        </div>
      `;
      paginationEl.innerHTML = '';
      return;
    }

    const start = (currentPage - 1) * PAGE_SIZE;
    const page = filtered.slice(start, start + PAGE_SIZE);

    listEl.innerHTML = page.map((q: any, i: number) => {
      const num = start + i + 1;
      return `
        <div class="bank-question">
          <div class="bank-q-header">
            <span class="bank-q-num">${num}</span>
            ${currentChapter === 'all' ? `<span class="bank-q-chapter">CH ${String(q.chapterId).padStart(2, '0')}</span>` : ''}
            <span class="bank-q-text">${highlight(q.question)}</span>
          </div>
          <div class="bank-options">
            ${q.options.map((opt: string, oi: number) => `
              <div class="bank-option ${oi + 1 === q.answer ? 'is-answer' : ''}">
                <span class="bank-opt-label">${labels[oi]}.</span>
                <span>${highlight(opt)}</span>
                ${oi + 1 === q.answer
                  ? '<svg width="14" height="14" viewBox="0 0 20 20" fill="none" style="margin-left:auto"><path d="M4 10.5L8 14.5L16 5.5" stroke="#16a34a" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>'
                  : ''}
              </div>
            `).join('')}
          </div>
        </div>
      `;
    }).join('');

    renderPagination(totalPages, filtered.length);
  }

  function updateSearchStats(count: number) {
    if (searchQuery) {
      searchStats.hidden = false;
      searchStats.textContent = `找到 ${count} 題`;
      searchClear.hidden = false;
    } else {
      searchStats.hidden = true;
      searchClear.hidden = true;
    }
  }

  function renderPagination(totalPages: number, total: number) {
    const start = (currentPage - 1) * PAGE_SIZE + 1;
    const end = Math.min(currentPage * PAGE_SIZE, total);

    let html = '<div class="page-bar">';
    html += `<div class="page-summary">
      <span class="page-summary-text">第 <strong>${start}</strong> - <strong>${end}</strong> 題</span>
      <span class="page-summary-total">共 ${total} 題 / ${totalPages} 頁</span>
    </div>`;

    if (totalPages > 1) {
      html += '<div class="page-buttons">';
      html += `<button class="page-btn" data-page="prev" ${currentPage <= 1 ? 'disabled' : ''}>
        <svg width="16" height="16" viewBox="0 0 20 20" fill="none"><path d="M12 4L6 10L12 16" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </button>`;

      const pages = getPageNumbers(currentPage, totalPages);
      for (const p of pages) {
        if (p === '...') {
          html += '<span class="page-ellipsis">...</span>';
        } else {
          html += `<button class="page-btn ${p === currentPage ? 'active' : ''}" data-page="${p}">${p}</button>`;
        }
      }

      html += `<button class="page-btn" data-page="next" ${currentPage >= totalPages ? 'disabled' : ''}>
        <svg width="16" height="16" viewBox="0 0 20 20" fill="none"><path d="M8 4L14 10L8 16" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </button>`;
      html += '</div>';
    }

    html += '</div>';
    paginationEl.innerHTML = html;
  }

  function getPageNumbers(current: number, total: number): (number | '...')[] {
    if (total <= 7) return Array.from({ length: total }, (_, i) => i + 1);
    const pages: (number | '...')[] = [1];
    const range = 1;
    const left = Math.max(2, current - range);
    const right = Math.min(total - 1, current + range);
    if (left > 2) pages.push('...');
    for (let i = left; i <= right; i++) pages.push(i);
    if (right < total - 1) pages.push('...');
    pages.push(total);
    return pages;
  }

  // Chapter nav clicks
  navBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      navBtns.forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      currentChapter = btn.dataset.ch === 'all' ? 'all' : Number(btn.dataset.ch);
      currentPage = 1;
      render();
    });
  });

  // Pagination clicks (delegated)
  paginationEl.addEventListener('click', (e) => {
    const btn = (e.target as HTMLElement).closest('.page-btn') as HTMLButtonElement;
    if (!btn) return;
    const val = btn.dataset.page;
    if (val === 'prev') currentPage--;
    else if (val === 'next') currentPage++;
    else currentPage = Number(val);
    render();
    window.scrollTo({ top: 0, behavior: 'smooth' });
  });

  // Search input (debounced)
  let searchTimer: number | undefined;
  searchInput.addEventListener('input', () => {
    window.clearTimeout(searchTimer);
    searchTimer = window.setTimeout(() => {
      searchQuery = searchInput.value.trim();
      currentPage = 1;
      render();
    }, 150);
  });

  searchInput.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && searchInput.value) {
      e.preventDefault();
      searchInput.value = '';
      searchQuery = '';
      currentPage = 1;
      render();
    }
  });

  searchClear.addEventListener('click', () => {
    searchInput.value = '';
    searchQuery = '';
    currentPage = 1;
    render();
    searchInput.focus();
  });

  render();
</script>
